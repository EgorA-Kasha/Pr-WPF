<Page x:Class="Pr_WPF.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pr_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="10">
            <Button Content="Назад" Click="Back_Click" Padding="10,5" DockPanel.Dock="Left"/>
            <Button Content="Выйти" Click="Logout_Click" Padding="10,5" Margin="10,0,10,0" DockPanel.Dock="Left"/>
            <Button Content="Сохранить" Click="Save_Click" Padding="10,5" DockPanel.Dock="Right"/>
        </DockPanel>

        <DataGrid Grid.Row="1" x:Name="GridUsers" Margin="10" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding Password}"/>
                <DataGridTextColumn Header="Роль (ID)" Binding="{Binding RoleID}"/>
                <DataGridCheckBoxColumn Header="Заморожен" Binding="{Binding IsFrozen}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
