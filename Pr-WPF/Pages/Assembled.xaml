<Page x:Class="Pr_WPF.Pages.Assembled"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pr_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Assembled" Loaded="Page_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Список сборок" Grid.Column="0">
            <ListView x:Name="LvAssemblies" SelectionChanged="LvAssemblies_SelectionChanged" BorderThickness="0" HorizontalContentAlignment="Stretch">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="{Binding name}" FontWeight="Bold" FontSize="16" TextWrapping="Wrap"/>
                            <TextBlock Grid.Row="1" Text="{Binding author, StringFormat={}Автор: {0}}" Foreground="Gray"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </GroupBox>

        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="LightGray"/>

        <GroupBox Header="Комплектующие сборки" Grid.Column="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListView x:Name="LvAssemblyParts" Grid.Row="0" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="4*"/>
                                    <ColumnDefinition Width="2*"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Source="{Binding image}" Stretch="Uniform"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding name}" FontWeight="Bold" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding manufacturer.name}" Foreground="Gray"/>
                                    <TextBlock Text="{Binding Characteristics}" TextWrapping="Wrap" FontSize="11" Foreground="#555555"/>
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="{Binding price, StringFormat={}{0:N0} ₽}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock x:Name="TxtTotal" Grid.Row="1" Text="Итоговая цена: 0 ₽" FontWeight="Bold" FontSize="18" HorizontalAlignment="Right"/>
            </Grid>
        </GroupBox>
    </Grid>
</Page>
