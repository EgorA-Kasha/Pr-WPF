<Page x:Class="Pr_WPF.Pages.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pr_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductsPage">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="10">
            <Button Content="Назад" Click="Back_Click" Padding="10,5" DockPanel.Dock="Left"/>
            <Button Content="Корзина" Click="Cart_Click" Padding="10,5" DockPanel.Dock="Right" Margin="10,0,0,0"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBox x:Name="TxtSearch" Width="200" TextChanged="Filter_Changed" Margin="5" Padding="2"/>
                <ComboBox x:Name="ComboType" Width="150" SelectionChanged="Filter_Changed" Margin="5"/>
                <ComboBox x:Name="ComboManuf" Width="150" SelectionChanged="Filter_Changed" Margin="5"/>
            </StackPanel>
        </DockPanel>

        <ScrollViewer Grid.Row="1" Margin="10">
            <ItemsControl x:Name="ListProducts">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="200" Margin="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Background="{Binding BgColor}">
                            <StackPanel Margin="10">
                                <TextBlock Text="{Binding Prod.Name}" FontWeight="Bold" TextWrapping="Wrap" Height="40"/>
                                <TextBlock Text="{Binding Prod.Manufacturer.Name}"/>
                                <TextBlock Text="{Binding Prod.Price, StringFormat=Цена: {0}}"/>
                                <TextBlock Text="{Binding Prod.Discount, StringFormat=Скидка: {0}%}"/>
                                <TextBlock Text="{Binding Prod.Rating, StringFormat=Оценка: {0}}"/>
                                <Button Content="В корзину" Click="AddToCart_Click" Tag="{Binding Prod}" Margin="0,10,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
