<Page x:Class="Pr_WPF.Pages.Ordering"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pr_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="800"
      Title="Ordering">

    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock x:Name="tb_userdata" Width="300" Height="40" />
            <ListBox ScrollViewer.HorizontalScrollBarVisibility="Disabled" x:Name="lb_ticket">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Margin="3" Text="Название:"/>
                            <TextBlock Margin="3" Text="{Binding movie.name}"/>
                            <TextBlock Margin="3" Text="Номер зала:"/>
                            <TextBlock Margin="3" Text="{Binding screening.room}"/>
                            <TextBlock Margin="3" Text="Дата показа:"/>
                            <TextBlock Margin="3" Text="{Binding screening.datetime}"/>
                            <TextBlock Margin="3" Text="Места:"/>
                            <TextBlock Margin="3" Text="{Binding seat}"/>
                            <TextBlock Margin="3" Text="Цена:"/>
                            <TextBlock Margin="3" Text="{Binding price}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button x:Name="button_order" Content="Заказать" Click="button_order_Click" />
        </StackPanel>
    </Grid>
</Page>
