<Page x:Class="Pr_WPF.Pages.Assemblying"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pr_WPF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Assemblying"
      Loaded="Page_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="TxtSearch" Grid.Column="0" VerticalContentAlignment="Center" TextChanged="FilterChanged" />
                <ComboBox x:Name="CmbManufacturer" Grid.Column="1" SelectionChanged="FilterChanged" DisplayMemberPath="name"/>
            </Grid>

            <TabControl x:Name="CategoriesTabControl" Grid.Row="1" SelectionChanged="CategoriesTabControl_SelectionChanged">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding name}" />
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <ListView ItemsSource="{Binding FilteredParts, RelativeSource={RelativeSource AncestorType=Page}}" 
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  MouseDoubleClick="CatalogItem_MouseDoubleClick"
                                  BorderThickness="0"
                                  HorizontalContentAlignment="Stretch">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="4*"/>
                                            <ColumnDefinition Width="2*"/>
                                        </Grid.ColumnDefinitions>
                                        <Image Grid.Column="0" Source="{Binding image}" Stretch="Uniform"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding name}" FontWeight="Bold" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding manufacturer.name}" Foreground="Gray"/>
                                            <TextBlock Text="{Binding Characteristics}" TextWrapping="Wrap" FontSize="11" Foreground="#555555"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="2" Text="{Binding price, StringFormat={}{0:N0} ₽}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
        </Grid>

        <GridSplitter Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="LightGray" Width="5"/>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Текущая сборка" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>

            <ListView x:Name="LvCart" Grid.Row="1" MouseDoubleClick="LvCart_MouseDoubleClick" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderThickness="0" HorizontalContentAlignment="Stretch">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="2*"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Source="{Binding image}" Stretch="Uniform"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding name}" FontWeight="Bold" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding manufacturer.name}" Foreground="Gray"/>
                                <TextBlock Text="{Binding Characteristics}" TextWrapping="Wrap" FontSize="11" Foreground="#555555"/>
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="{Binding price, StringFormat={}{0:N0} ₽}" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" x:Name="TxtTotalPrice" Text="Итого: 0 ₽" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right"/>
                <TextBox Grid.Row="1" x:Name="TxtAssemblyAuthor" Text="Имя автора"/>
                <TextBox Grid.Row="2" x:Name="TxtAssemblyName" Text="Название сборки"/>
                <Button Grid.Row="3" Content="Сохранить сборку" Background="#FF4CAF50" Foreground="White" FontWeight="Bold" Click="BtnSaveAssembly_Click" Cursor="Hand"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
