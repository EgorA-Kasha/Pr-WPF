<UserControl x:Class="CarConfigurator.Step1Page"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel Margin="20">
        <TextBlock x:Name="titleText" 
                   Text="Выберите модель автомобиля" 
                   FontSize="16" FontWeight="SemiBold" 
                   Margin="0,0,0,10"/>

        <ComboBox x:Name="modelCombo"
                  FontSize="14" Padding="10,5"
                  Margin="0,5,0,15" MinWidth="200"/>

        <TextBlock x:Name="subtitleText"
                   Text="Выберите тип двигателя" 
                   FontSize="16" FontWeight="SemiBold" 
                   Margin="0,20,0,10"/>

        <ComboBox x:Name="engineCombo"
                  FontSize="14" Padding="10,5"
                  Margin="0,5,0,15" MinWidth="200"/>

        <StackPanel x:Name="pricePanel" Margin="0,20,0,0">
            <TextBlock x:Name="priceLabel"
                       Text="Базовая цена:" 
                       FontSize="14" Margin="0,10,0,5"/>

            <TextBlock x:Name="basePriceText"
                       FontSize="16" FontWeight="Bold"
                       Foreground="DarkGreen" Margin="0,5"/>
        </StackPanel>
    </StackPanel>
</UserControl>